// export const isRealString = (str) => {
//   return typeof str === "string" && str.trim().length > 0;
// };

// export const generatePiece = (max, min) => {
//   return Math.floor(Math.random() * (max - min) + min);
// };
